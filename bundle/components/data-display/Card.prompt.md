Content surface card — white with soft shadow and 1px border. `peach` for warm product cards, `dark` for Zuper Sense. Set `hoverable` to float on hover.

```jsx
<Card hoverable>
  <Eyebrow>Intelligent Quoting</Eyebrow>
  <h3>Quotes that win, on the spot</h3>
</Card>
<Card variant="peach">…</Card>
```
