Native select styled to match Zuper inputs, with a line chevron.

```jsx
<Select label="Industry" options={['Roofing','HVAC','Plumbing','Electrical']} />
<Select label="Status" options={[{value:'open',label:'Open'},{value:'done',label:'Closed'}]} />
```
