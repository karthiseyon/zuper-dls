Text input with label, hint, error state and optional leading line-icon. Orange focus ring.

```jsx
<Input label="Work email" placeholder="you@company.com" hint="We'll never share it." />
<Input label="Job site" error="Required" />
```
