import * as React from 'react';

/**
 * Recreated Zuper dispatch-board product screen for use in creatives at any ratio.
 * @startingPoint section="Product screens" subtitle="Dispatch, quote, costing & mobile" viewport="700x420"
 */
export interface DispatchBoardProps { style?: React.CSSProperties; }
export function DispatchBoard(props: DispatchBoardProps): JSX.Element;
