import * as React from 'react';
/** Recreated Zuper mobile app field-job view (phone frame) for creatives. */
export interface MobileJobProps { style?: React.CSSProperties; }
export function MobileJob(props: MobileJobProps): JSX.Element;
